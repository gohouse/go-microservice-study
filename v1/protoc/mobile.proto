// 请认真对待 proto 文件的文件名，常用规则 MessageName.proto
// 故此包名, 命名为 mobile.proto
syntax = "proto3"; // 指定语法格式，proto3
package protoc; // 指定生成的 mobile.pb.go 的包名，防止命名冲突

message Mobile {
    string brand = 1; // 手机品牌
}

